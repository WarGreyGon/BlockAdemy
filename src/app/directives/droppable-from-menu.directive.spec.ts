import { DroppableFromMenuDirective } from './droppable-from-menu.directive';

describe('DroppableFromMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new DroppableFromMenuDirective();
    expect(directive).toBeTruthy();
  });
});
