import { DraggableInWhiteboardDirective } from './draggable-in-whiteboard.directive';

describe('DraggableInWhiteboardDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableInWhiteboardDirective();
    expect(directive).toBeTruthy();
  });
});
